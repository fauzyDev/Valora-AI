# src/app/layout.tsx

[Lihat sumber](src/app/layout.tsx)

Ringkasan:

Root layout Next.js yang mengatur metadata global, font, dan pembungkus aplikasi seperti `TooltipProvider` dan `Toaster`.

Ekspor:

- `viewport` — konfigurasi viewport.
- `metadata` — metadata situs.
- Default export: `RootLayout({ children })`.
