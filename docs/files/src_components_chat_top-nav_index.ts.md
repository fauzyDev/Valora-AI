# src/components/chat/top-nav/index.ts

[Lihat sumber](src/components/chat/top-nav/index.ts)

Ringkasan:

Re-export untuk `TopNav` agar import dari direktori lebih singkat.

Ekspor:

- `TopNav` (re-export dari `TopNav.tsx`).
