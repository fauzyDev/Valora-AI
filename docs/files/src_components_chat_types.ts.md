# src/components/chat/types.ts

[Lihat sumber](src/components/chat/types.ts)

Ringkasan:

Tipe TypeScript untuk `Attachment`, `Message`, dan `ChatSession` yang dipakai di seluruh modul chat.

Ekspor:

- `Attachment`, `Message`, `ChatSession` (interface TypeScript).
