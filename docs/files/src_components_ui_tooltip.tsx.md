 # src/components/ui/tooltip.tsx

 [Lihat sumber](src/components/ui/tooltip.tsx)

 Ringkasan:

 Pembungkus kecil untuk primitive Tooltip. Menyediakan `Tooltip`, `TooltipTrigger`, `TooltipContent`, dan `TooltipProvider` yang meneruskan props dan children.

 Ekspor:

 - `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider`.
# src/components/ui/tooltip.tsx

[Lihat sumber](src/components/ui/tooltip.tsx)

Ringkasan:

Wrappers kecil di atas primitive Tooltip dari `@base-ui/react/tooltip`. Menyediakan `Tooltip`, `TooltipTrigger`, `TooltipContent`, dan `TooltipProvider` untuk konsistensi penggunaan tooltip di aplikasi.

Ekspor:

- `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider`.
