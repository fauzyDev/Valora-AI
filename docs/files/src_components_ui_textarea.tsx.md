 # src/components/ui/textarea.tsx

 [Lihat sumber](src/components/ui/textarea.tsx)

 Ringkasan:

 Komponen `Textarea` yang dibungkus dengan styling dan props tambahan seperti autosize, kelas utilitas, dan forwarding ref.

 Ekspor:

 - Default export: `Textarea`.
# src/components/ui/textarea.tsx

[Lihat sumber](src/components/ui/textarea.tsx)

Ringkasan:

Pembungkus `<textarea>` dengan forwarding `ref` dan styling Tailwind yang konsisten. Digunakan untuk input chat agar dapat auto-resize dan terintegrasi dengan form.

Ekspor:

- `Textarea` (komponen React, forwardRef).
