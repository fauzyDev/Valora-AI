# src/app/error.tsx

[Lihat sumber](src/app/error.tsx)

Ringkasan:

Komponen fallback untuk error pada tingkat aplikasi. Menyediakan UI untuk menampilkan detail error dan opsi untuk reset atau kembali ke beranda.

Ekspor:

- Default export: `Error({ error, reset })`.
