 # src/components/ui/button.tsx

 [Lihat sumber](src/components/ui/button.tsx)

 Ringkasan:

 Wrapper untuk tombol dengan styling utilitas, mendukung `variant`, `size`, dan forwarding props serta `ref`.

 Ekspor:

 - Default export: `Button`.
# src/components/ui/button.tsx

[Lihat sumber](src/components/ui/button.tsx)

Ringkasan:

Pembungkus `Button` menggunakan `@base-ui/react/button` dengan variasi styling yang dihasilkan oleh `class-variance-authority` (`buttonVariants`). Mendukung berbagai ukuran dan varian tombol.

Ekspor:

- `Button` — komponen React.
- `buttonVariants` — helper varian kelas.
