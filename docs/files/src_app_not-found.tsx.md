 # src/app/not-found.tsx

 [Lihat sumber](src/app/not-found.tsx)

 Ringkasan:

 Komponen fallback untuk rute yang tidak ditemukan (404).

 Ekspor:

 - Default export: `NotFound`.
# src/app/not-found.tsx

[Lihat sumber](src/app/not-found.tsx)

Ringkasan:

Halaman 404 kustom yang menampilkan pesan "Page Not Found" dan tombol untuk kembali ke beranda.

Ekspor:

- Default export: komponen `NotFound`.
