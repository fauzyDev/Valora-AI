 # src/components/ui/badge.tsx

 [Lihat sumber](src/components/ui/badge.tsx)

 Ringkasan:

 Komponen badge kecil untuk menampilkan label singkat seperti status atau count.

 Ekspor:

 - Default export: `Badge`.
# src/components/ui/badge.tsx

[Lihat sumber](src/components/ui/badge.tsx)

Ringkasan:

Komponen `Badge` dengan varian gaya untuk label kecil/penanda. Mendukung prop `render` untuk mengganti tag HTML yang digunakan.

Ekspor:

- `Badge`, `badgeVariants`.
