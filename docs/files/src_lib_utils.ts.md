 # src/lib/utils.ts

 [Lihat sumber](src/lib/utils.ts)

 Ringkasan:

 Kumpulan utilitas kecil yang digunakan oleh modul lain, seperti helper string, tanggal, atau transformasi ringan.

 Ekspor:

 - Berbagai utilitas bantu (named exports) untuk operasi umum.
# src/lib/utils.ts

[Lihat sumber](src/lib/utils.ts)

Ringkasan:

Utilitas kecil untuk menggabungkan className: `cn(...)` menggunakan `clsx` + `tailwind-merge`.

Ekspor:

- `cn(...inputs: ClassValue[])` — helper untuk menggabungkan dan merapikan kelas Tailwind.
