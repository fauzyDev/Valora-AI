 # src/components/ui/avatar.tsx

 [Lihat sumber](src/components/ui/avatar.tsx)

 Ringkasan:

 Komponen avatar pengguna yang menampilkan gambar atau inisial, dengan fallback dan styling kecil.

 Ekspor:

 - Default export: `Avatar`.
# src/components/ui/avatar.tsx

[Lihat sumber](src/components/ui/avatar.tsx)

Ringkasan:

Grup avatar dan utilitas terkait — pembungkus untuk primitive Avatar dari `@base-ui/react/avatar`.

Ekspor:

- `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarGroup`, `AvatarGroupCount`, `AvatarBadge`.
