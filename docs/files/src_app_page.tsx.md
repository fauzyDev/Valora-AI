 # src/app/page.tsx

 [Lihat sumber](src/app/page.tsx)

 Ringkasan:

 Halaman landing root (server component) yang merender `VeloraLanding`.

 Ekspor:

 - Default export: `Home` atau `Page` (server component).
# src/app/page.tsx

[Lihat sumber](src/app/page.tsx)

Ringkasan:

Halaman utama yang merender `VeloraLanding`. Mendefinisikan metadata halaman (title, description).

Ekspor:

- `metadata` — objek `Metadata` Next.js.
- Default export: `Page()` — komponen halaman yang merender landing.
