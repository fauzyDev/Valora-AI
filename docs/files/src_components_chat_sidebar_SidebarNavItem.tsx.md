# src/components/chat/sidebar/SidebarNavItem.tsx

[Lihat sumber](src/components/chat/sidebar/SidebarNavItem.tsx)

Ringkasan:

Item navigasi kecil untuk sidebar, menampilkan ikon dan label, mendukung state `active`.

Ekspor:

- `SidebarNavItem` (komponen React).
